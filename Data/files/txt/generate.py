import csv

def search(fileName):
  print("Searching...")
  data = {}

  with open(fileName) as file: 
  
  # split
  # for data in file
  # startswith
  # strip
  # each dict value is an array, make an empty array if the dict list doesnt already exist, else append it
  
  print("Done!")
  return data

def run():
  data = search("Data/files/txt/books.txt")

#perhaps use a loop within a loop? goes into array and then loops
#through each item in the array to print it
run()