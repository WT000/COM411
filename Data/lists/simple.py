# Directions function that creates and returns the direction list
def directions():
  directions = ["Move Forward", "Move Backward", "Turn Left", "Turn Right"]
  return directions

# Run function that runs and displays the function
def run():
  print(directions())

# Run the program
run()